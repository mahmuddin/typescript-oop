describe('Interface', () => {
    interface HasName {
        name: string
    }

    interface CanSayHello {
        sayHello(name: string): void
    }

    it('should can create interface', () => {

    });
})